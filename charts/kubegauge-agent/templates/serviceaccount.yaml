# serviceaccount.yaml — the agent's read-only identity.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubegauge-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubegauge-agent.labels" . | nindent 4 }}
